// TODO: implement constants
import 'package:flutter/material.dart';

class AppColors {
  // Primary Brand Colors
  static const Color primary = Color(0xFF004D40);
  static const Color primaryDark = Color(0xFF00342B);
  static const Color primaryLight = Color(0xFF94D3C1);

  // Secondary
  static const Color secondary = Color(0xFF136964);

  // Accent
  static const Color accent = Color(0xFFF09E34);

  // Backgrounds
  static const Color background = Color(0xFFF9F9F9);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color surfaceVariant = Color(0xFFEEEEEE);

  // Text
  static const Color textPrimary = Color(0xFF1A1C1C);
  static const Color textSecondary = Color(0xFF3F4945);

  // Borders & Outline
  static const Color outline = Color(0xFFBFC9C4);

  // States
  static const Color error = Color(0xFFBA1A1A);
  static const Color success = Color(0xFF136964);

  // Shadows
  static const Color shadow = Colors.black12;
}