{
  "name": "meetmap-addis-firestore-rules-tests",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test firestore.rules.test.js",
    "test:emulator": "firebase emulators:exec --config ../firebase.json --only firestore --project demo-meetmap-addis \"npm test\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "5.0.2",
    "firebase": "12.19.0"
  }
}
